# Calculate the square of a number using a lambda function

square = lambda x:x * x
print(square(5))

# add the numbers using parameters and lambda function 

add = lambda a,b: a + b
print(add(10,20))